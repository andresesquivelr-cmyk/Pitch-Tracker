# Backend environment variables — copy to backend/.env and fill in your values

# From: Supabase project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co

# Use the service_role key (NOT the anon key) — keeps elevated access server-side only
SUPABASE_SERVICE_KEY=your-service-role-secret-key
